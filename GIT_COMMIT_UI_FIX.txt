fix: 统一文件夹删除弹窗文案和修复返回导航

修改内容：

1. 统一删除弹窗文案
   - 修改子文件夹删除弹窗，与根文件夹保持一致
   - 新文案：明确说明文件夹将被移至回收站，可在30天内恢复
   - 添加警告图标和颜色提示，提升用户体验

2. 修复根文件夹返回导航
   - 根文件夹详情页的返回按钮现在返回到文件夹管理页面
   - 之前：返回到仪表盘 (/dashboard)
   - 现在：返回到文件夹管理 (/folders)
   - 子文件夹返回逻辑保持不变（返回到父文件夹）

修改的文件：
- frontend/src/components/FolderDetail/SubFolderCard.jsx
  * 更新删除弹窗的 title 和 description
  * 添加格式化的标题和警告提示
  * 修改确认按钮文案为"确定删除"
  * 添加 okButtonProps 设置按钮为危险样式

- frontend/src/pages/FolderDetail.jsx
  * 修改根文件夹返回逻辑：navigate('/dashboard') -> navigate('/folders')
  * 保持子文件夹返回到父文件夹的逻辑不变

用户体验改进：
- 删除提示更加友好和一致
- 明确告知用户文件可恢复
- 导航逻辑更符合用户预期
